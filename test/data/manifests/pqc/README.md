# Tests for handling cross-distro builds with PQC keys

See https://issues.redhat.com/browse/HMS-10057

This is a RHEL 9.7 manifest configured so that it can be built on all (supported) host distributions: RHEL 9.7, RHEL 10.1, Fedora 43 (current).
Host distro is defined by the CI test runner.
