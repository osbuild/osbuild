#!/usr/bin/python3

import json
import sys

from osbuild.stage_utils import run_systemd_firstboot

if __name__ == '__main__':
    args = json.load(sys.stdin)
    sys.exit(run_systemd_firstboot(args["tree"], "hostname", args["options"]["hostname"], "/etc/hostname"))
