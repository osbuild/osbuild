summary: Configure greenboot
description: Update configuration of greenboot in /etc/greenboot/greenbot.conf.
schema:
  additionalProperties: false
  required:
  - config
  properties:
    config:
      additionalProperties: false
      description: greenboot config options
      type: object
      properties:
        monitor_services:
          type: array
          items:
            type: string
