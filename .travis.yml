dist: bionic
language: python
python:
  - "3.7"
before_install:
  - sudo apt-get install -y systemd-container tree
install:
  - pip install pylint
jobs:
  include:
    - name: pylint
      script: pylint osbuild assemblers/* stages/*
    - name: pipeline-noop
      script: sudo env "PATH=$PATH" python3 -m osbuild --libdir . --output . samples/noop.json
